version: '3.8'

services:
  frontend-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: frontend:dev
    container_name: frontend-dev
    ports:
      - "8081:80"
    restart: always
