version: '3.8'

services:
  frontend-prod:
    image: alifmaulanarizqi/react-frontend:latest
    container_name: react-frontend-prod
    ports:
      # Port 80 di VPS akan diarahkan ke port 80 (Nginx) di dalam container
      - "80:80"
    restart: always
