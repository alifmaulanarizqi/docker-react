version: '3.8'

services:
  frontend-prod:
    build:
      context: .
      dockerfile: Dockerfile
    image: frontend:latest
    container_name: frontend-prod
    ports:
      - "80:80"
    restart: always
